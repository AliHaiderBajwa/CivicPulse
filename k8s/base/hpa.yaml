# HPA computes utilisation as usage / request. Without resources.requests.cpu
# on the pods there is no denominator and this sits at <unknown>/60% forever.
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: backend
  namespace: civicpulse
  labels:
    app.kubernetes.io/part-of: civicpulse
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: backend
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300 # scale down slowly — flapping is expensive
    scaleUp:
      stabilizationWindowSeconds: 0   # scale up immediately — users are waiting
